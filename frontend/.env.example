# Frontend environment (Next.js). Copy to .env.local and adjust.
# These are server-only: the management secret never reaches the browser.

# Base URL of the FastAPI backend.
BACKEND_URL=http://localhost:8000

# Shared secret required by the backend's document-management endpoints.
# Must match MANAGEMENT_SECRET in the backend .env.
MANAGEMENT_SECRET=change-me
